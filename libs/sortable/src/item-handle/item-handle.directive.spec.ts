/* tslint:disable:no-unused-variable */

import { async, inject } from '@angular/core/testing';
import { ItemHandleDirective } from './item-handle.directive';

describe('Directive: SortableItemHandle', () => {
  it('should create an instance', () => {
    const directive = new ItemHandleDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
